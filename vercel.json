{
  "buildCommand": "DATABASE_URL=\"postgresql://localhost:5432/local_db\" prisma generate && next build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "redirects": []
}
